请先阅读当前项目中的 `/Users/draw/Documents/New project-20260624/pinterest/` 目录，重点阅读其中的 `design.md`。

请使用 `pinterest/design.md` 中定义的设计系统，对当前项目的七牛校招活动页重新进行视觉设计和前端实现。

执行前必须先检查：
1. 当前 Git 分支必须是 `main-v2`
2. 工作区必须干净，包括 tracked 和 untracked 文件都不能有变更

如果当前分支不是 `main-v2`，或工作区存在任何未提交/未跟踪变更，请立即停止，并告诉我检查结果，不要继续生成或修改代码。

设计要求：
- 只基于当前 `main-v2` 分支中的项目文件、页面内容和结构化数据进行设计
- 不参考之前分支、之前修改结果或当前对话中的旧方案
- 不保留原代码中的视觉风格
- 页面内容、核心信息、CTA、模块和功能需要完整保留
- 视觉风格必须遵循 `pinterest/design.md` 的设计系统
- 保持现有技术栈：Vite + React + Tailwind CSS
- 修改后运行 `npm run build`，确保构建通过